---
phase: 10.2-static-audit-workflow
verified: 2026-09-14T00:00:00Z
status: gaps_found
score: 4/5 must-haves fully verified; 1/5 partially verified (named gap, not silently dropped)
covered_files:
  - .planning/phases/10.2-static-audit-workflow/10.2-01-PLAN.md
  - .planning/phases/10.2-static-audit-workflow/10.2-01-SUMMARY.md
  - core/database/src/main/kotlin/com/nadeem/apkscope/core/database/SecurityAuditEntities.kt
  - app/src/main/java/com/nadeem/apkscope/domain/SecurityAuditRepository.kt
  - app/src/main/java/com/nadeem/apkscope/ui/screens/securityaudit/SecurityAuditViewModel.kt
  - app/src/main/java/com/nadeem/apkscope/ui/screens/securityaudit/SecurityAuditScreen.kt
  - app/src/main/java/com/nadeem/apkscope/ui/screens/securityaudit/SecurityAuditFindingDetailScreen.kt
covered_digest: "not computed — verification below is a direct on-device tap-through reconciliation, not a digest comparison"
behavior_unverified: 1
behavior_unverified_items:
  - truth: "Cancelling an in-progress audit and an audit that hits a collection failure each produce their own distinct, non-misleading state (RUNNING→CANCELLED, and an ERROR state) rather than a silent clean report"
    test: "Trigger runAudit() and call cancel() before the coroutine reaches its single atomic Room write; separately, force AnalysisNotFoundException or another Exception path and assert the ERROR state renders with a retry action"
    expected: "CANCELLED state renders 'Audit cancelled' with a re-run action and no persisted row; ERROR state renders the failure message with a retry action; neither is reachable through manual on-device tap-through because a static audit completes too fast to interrupt"
    why_human: "No automated instrumented test exists yet for this specific race — the code path is implemented (see SecurityAuditViewModel's structured-concurrency cancellation before its one atomic write) but not exercised by any test, manual or automated, this pass"
coincidental_reliance_items: []
---

# Phase 10.2: Static Security Audit — First Complete User Workflow Verification Report

**Phase Goal:** A real, on-device, end-to-end workflow — select APK, run static audit, view findings/
coverage, open finding evidence/remediation, persist, restart the app, reopen the same audit.
**Verified:** 2026-09-14
**Status:** gaps_found — one non-happy-path truth (item 5 of the plan's must-haves) is implemented but not
exercised by any test, automated or manual. Every happy-path truth is genuinely product-verified.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | "Run Security Audit" reachable from Analysis Detail, no install/Work-Profile step | ✓ VERIFIED | Real device: tapped the new row, `SecurityAuditScreen` opened in `EMPTY` state, "Run Security Audit" completed with zero sandbox interaction |
| 2 | Findings render with outcome/severity/confidence + coverage summary | ✓ VERIFIED | Real device: `2 Findings / 4 Review / 4 Passed / 0 Untested` (sums to 10), all 10 findings' titles/badges matched Risk Signal Fixture's actual manifest |
| 3 | Opening a finding shows evidence + remediation | ✓ VERIFIED | Real device: "Debuggable build" detail showed distinct Evidence/Remediation cards, rule ID, origin note |
| 4 | Audit survives a real restart, reopens identical, no re-run | ✓ VERIFIED | Real device: genuine `am force-stop` + relaunch; reopened directly into `LOADED` with byte-identical content |
| 5 | Loading/empty/running/cancelled/error states are each implemented and distinct | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | See `behavior_unverified_items` — `LOADING_PERSISTED`/`EMPTY`/`LOADED` verified; `RUNNING`/`CANCELLED`/`ERROR` implemented, not exercised |

**Score:** 4/5 truths fully product-verified; 1/5 present and wired but behaviorally unverified —
explicitly named, not silently marked complete.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `SecurityAuditEntities.kt` | Room entities + DAO, mirrors `FinalReportEntities.kt` | ✓ EXISTS + SUBSTANTIVE | Compiles, used by repository and confirmed via real Room writes/reads on-device |
| `SecurityAuditRepository.kt` | run-and-persist, observe, get | ✓ EXISTS + SUBSTANTIVE | Exercised end-to-end on real device (run, then restart-and-read-back) |
| `SecurityAuditViewModel.kt` | 6-state machine | ✓ EXISTS + SUBSTANTIVE | 3 of 6 states device-confirmed; 3 present in source, untested |
| `SecurityAuditScreen.kt` | Findings list + coverage UI | ✓ EXISTS + SUBSTANTIVE | Rendered correctly on real device with real data |
| `SecurityAuditFindingDetailScreen.kt` | Evidence/remediation detail | ✓ EXISTS + SUBSTANTIVE | Rendered correctly on real device |

**Artifacts:** 5/5 verified.

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `StaticResultScreen` | `SecurityAuditRoute` | `InspectionActionRow.onClick` | ✓ WIRED | Confirmed by real tap navigation |
| `SecurityAuditScreen` | `SecurityAuditRepository.runAndPersistAudit` | `viewModel.runAudit()` | ✓ WIRED | Confirmed: real findings appeared after tapping "Run Security Audit" |
| `SecurityAuditRepository` | Room (`SecurityAuditDao`) | `upsertAudit` | ✓ WIRED | Confirmed by restart-and-reopen showing persisted, not re-computed, data |
| `SecurityAuditScreen` | `SecurityAuditFindingDetailScreen` | `onOpenFinding` | ✓ WIRED | Confirmed by real tap navigation to finding detail |
| `SecurityAuditRepository` | `AnalysisSessionEntity`/`RiskFindingEntity` (existing risk score) | (intentionally none) | ✓ NOT WIRED (correct) | MS10-FOUND04: confirmed the risk badge was unchanged before/after running the audit |

**Wiring:** 5/5 verified (including one deliberate non-wiring, confirmed correct).

## Commands run this verification pass

```bash
./gradlew test testDebugUnitTest            # tests=394 failures=0
./gradlew :app:assembleDebug                # BUILD SUCCESSFUL
adb devices -l                              # emulator-5554 confirmed connected
adb -s emulator-5554 install -r ...         # Success
adb -s emulator-5554 push riskfixture-debug.apk /sdcard/Download/
# (real tap-through via adb shell input tap, uiautomator dump for exact element bounds,
#  adb shell am force-stop + am start for the restart test — full transcript in STATE.md)
```

## Conclusion

Phase 10.2 delivered a genuinely product-verified user workflow — the specification's own explicit
distinction ("a rule test is component evidence, not genuine product execution evidence") is respected
here: every claim above cites an actual on-device action, not a unit test. The one gap (non-happy-path
states) is named, scoped, and left open rather than glossed over — MS10-UI05 in `.planning/REQUIREMENTS.md`
is marked "source implemented, partially product verified," not "done."
