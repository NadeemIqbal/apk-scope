package com.nadeem.apkscope.sandbox

/** The app's real `FileProvider` authority (declared in `AndroidManifest.xml`) — one named constant instead of the string literal repeated across every cross-profile call site. */
const val SANDBOX_FILE_PROVIDER_AUTHORITY = "com.nadeem.apkscope.files"
